{
  "name": "todo-app",
  "version": "1.0.0",
  "type": "module",
  "description": "Tasker — a small todo app demoing Quant0 hosted-redirect sign-in via plain OAuth Authorization Code + PKCE (no SDK). Not an npm workspace member; install standalone.",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-router-dom": "^7.15.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "vite": "^7.3.2"
  }
}
